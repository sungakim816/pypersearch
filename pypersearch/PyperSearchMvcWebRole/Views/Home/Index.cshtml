@{
    ViewBag.Title = "Home";
}
<div id="landing">
    <br /><br /><br /><br /><br /><br />
    <div class="container">
        <div class="jumbotron text-center bg-transparent">
            <h1>Pyper Search</h1>
            <p class="lead text-lg-center text-dark">Pyper Search is an open source search engine using Azure Cloud Service as a Platform.</p>
            <p>Start your search here...</p>
        </div>
        <div class="d-flex justify-content-center">@Html.Partial("_MainSearchBar")</div>
    </div>
</div>

@section scripts {
    <script>
        $("#home-tab").addClass("active");
        function showQuerySuggestions(str) {
            if (str.length == 0) {
                return 0;
            }
        }
    </script>
}